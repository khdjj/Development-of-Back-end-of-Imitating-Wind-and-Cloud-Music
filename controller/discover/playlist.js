let playListDao = require('../../dao/playlistDao');

class PlayList {

    constructor(){
        this.getPlayList = this.getPlayList.bind(this);
    }

    getPlayList(req,res,next){
        
    }
}

export default new PlayList();